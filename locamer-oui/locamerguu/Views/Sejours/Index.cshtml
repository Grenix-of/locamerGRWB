@model IEnumerable<locamerguu.Sejour>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.dateDebut)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dateFin)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Client.nomCli)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Type.libelle)
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.dateDebut)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dateFin)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Client.nomCli)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Type.libelle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idSejour }) |
            @Html.ActionLink("Details", "Details", new { id=item.idSejour }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idSejour })
        </td>
    </tr>
}

</table>
